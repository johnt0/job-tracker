[project]
name = "job-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.7",
    "django-stubs>=6.0.7",
    "djangorestframework>=3.17.1",
    "django-environ",
    "psycopg[binary]",
    "gunicorn",
    "whitenoise",
]
